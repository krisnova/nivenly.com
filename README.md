# Nivenly.com

Built on [Grav CMS](https://getgrav.org/).

Written (proudly) in PHP.

### Volume Mounts

``` 
.
└── public
    ├── bin
    ├── logs
    ├── system
    └── user
        ├── accounts (secret)
        ├── config   (secret)
        ├── data     (secret)
        ├── pages    (public)
        ├── plugins  (secret)
        └── themes   (secret)
```